# HANDOFF — статус: ВЫПОЛНЕНО (2026-09-06)

Все пункты плана закрыты. Этот файл — исторический артефакт передачи контекста;
актуальная документация — в `README.md` и `POINT_ELEMENTS_ANALYSIS.md`.

Итоги:

1. ✅ Баги RVP/Cam исправлены: (а) строки `ani *b_01 0` вне секции `[Track]`
   больше не затирают preparetrack-биндинги (второй токен биндинга обязан
   начинаться с `*`); блоки `*magic N` не создают фантомные объекты;
   (б) пустые 228-байтные Dummy-блоки парсятся как статичные якоря;
   (в) bounds-check счётчиков учитывает усечение последнего ключа (−4 байта);
   плюс защита чтения ключей от обрыва файла.
2. ✅ Найден и исправлен дополнительный баг: хвостовой ключ-терминатор `.cam`
   с `frame=0` (дубль финальной позы) давал телепорт объекта на t=0 —
   теперь такие ключи отбрасываются (`CamFile.DropSentinelKeys`).
3. ✅ Найден и исправлен старый баг BSP-муверов: `GetFrameAlphaCore`
   (`nowFrame <= first` → `<`) — кадр 0 при первом ключе в кадре 0 отдавал
   последний ключ вместо первого (снап муверов на старте анимации).
4. ✅ Тесты: Platform01 (26 RVP-объектов, no-dummy=0), neutralA/B/C+S1/S2,
   accretia, bellato, cora, Dungeon04. Батч 41/41, `--validate` code=1 везде,
   code=2 только у Dungeon00 (известное).
5. ✅ Release-билд, пакет `RFMapToolSharp-portable/app/` обновлён,
   smoke-тест portable (Platform01 через `rf_path.txt` с 8.3-путём) пройден,
   `RFMapToolSharp-portable.zip` пересобран (33,1 МБ).
6. ✅ README.md и POINT_ELEMENTS_ANALYSIS.md обновлены (FX/SND из `.ebp`,
   RVP из `.cam`, формат `.cam`, ограничения).

Известные остатки (не блокеры): часть `.msh` (bone-меши) падает в
fallback-маркеры («not enough triangles»); картовых `.R3E`-частиц и папки
`Character\` в данных нет; коллизии не экспортируются; сканеры
`tools/scan_anim.py`/`scan_objpos.py` не знают класс нод `RVP_*`
(флаги на них — ожидаемый шум, позиции/треки сверены с исходниками).
